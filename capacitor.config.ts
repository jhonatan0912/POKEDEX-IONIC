import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.pokedex-ionic',
  appName: 'pokeapi',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
